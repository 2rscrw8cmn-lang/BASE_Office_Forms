# Root README Update

Add the following section to the root `README.md` after the backup/import discussion and before “Try it now”:

```markdown
## Product architecture and roadmap

The implementation source of truth for projects, RFIs, submittals, controlled documents,
files, sharing, security, AI, and rollout is [`docs/README.md`](docs/README.md).
```

This file exists because the architecture package may be applied independently. Remove this helper after the root README has been updated.
